<%= form_for(@antique) do |f| %>
  <% if @antique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@antique.errors.count, "error") %> prohibited this antique from being saved:</h2>

      <ul>
      <% @antique.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
