<p id="notice"><%= notice %></p>

<h1>Mis mensajes</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Anuncio</th>
      <th>Asunto</th>
      <th>Mensaje</th>
      <th>Email</th>
      <th>Teléfono</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @messages_property.each do |contact| %>
      <tr <%
              if contact.is_read != 0 %>
                 class="info"
           <% end
          %>
        >
        <td><%= contact.property.title %></td>
        <td><%= contact.subject %></td>
        <td><%= contact.body %></td>
        <td><%= contact.email %></td>
        <td><%= contact.phone %></td>
        <td><%= link_to 'Eliminar', contact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>